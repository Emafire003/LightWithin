---
title: LightWithin Commands
---

## /light reload
**Permission required:** `lightwithin.commands.reload`

Reloads the config files, and upload the values from the file to the memory, so the new values are used. There is an anlogous for the client, which is `/ligth_client reload` (reloads the ClientConfig, much like editing it with YACL).